import 'package:flutter/material.dart';

const Color primaryColor = Color(0xFF40BFFF);

const Color primaryRed = Color(0xFFFB7181);
const Color primaryYellow = Color(0xFFFFC833);
const Color primaryGreen = Color(0xFF53D1B6);
const Color primaryPurple = Color(0xFF5C61F4);

const Color bgColor = Color(0xFFFFFFFF);

const Color neutralGrey = Color(0xFF9098B1);
const Color neutralDark = Color(0xFF223263);
//Design Color Is : FFEBF0FF
const Color neutralLight = Color(0xFFE2E7F8);

const double bigRadus = 16.0;
const double smallRadus = 5.0;
const double defualtPadding = 20;

const BorderRadius defulutBorderRadius =
    BorderRadius.all(Radius.circular(smallRadus));
const BorderRadius bigBorderRadius =
    BorderRadius.all(Radius.circular(bigRadus));
List<Color> colorsList = [
  primaryYellow,
  primaryColor,
  primaryRed,
  primaryGreen,
  primaryPurple,
  neutralDark
];
